# AI Instructions (Repo)

You are helping me build and maintain this codebase.

## Non-negotiables
- Prefer simple, readable solutions.
- Do not add new dependencies unless I explicitly approve.
- Ask clarifying questions if requirements are ambiguous.
- When refactoring: do not change behavior unless requested.

## Output format
- Propose a plan before large changes.
- Provide small, reviewable diffs.
- Include tests when practical.

## TypeScript/ESM note
- This repo uses ESM + NodeNext typechecking; relative imports in TS files should include `.js` extensions (e.g. `../src/foo.js`).
